# Dockerfile.frontend

# 使用 Node.js 官方镜像作为基础镜像
FROM node:14

# 设置工作目录
WORKDIR /app

# 复制 package.json 和 package-lock.json 或 yarn.lock
COPY frontend/package*.json ./

# 安装依赖项
RUN npm install

# 复制所有前端项目文件到工作目录
COPY frontend ./

# 公开端口（React 默认开发服务器端口）
EXPOSE 3000

# 启动前端应用
CMD ["npm", "start"]
